<% if @feed_items.any? %>
  <ol class="microposts">
    <% if !@feed_items.empty? do %>
    <%= render partial: 'shared/feed_item', collection: @feed_items %>
    <% else %>
    no posts yet
    <% end %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>